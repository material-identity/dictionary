{
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "$id": "https://dictionary1.eu/schema/dictionary-entry.json",
  "title": "DPP dictionary entry",
  "description": "Response shape returned by GET <dictionaryReference>. Every published entry is immutable forever. Supersession is expressed only by a newer entry's `replaces` link; nothing tracks status, versions, or lifecycle anywhere. 'Current' and 'superseded' are derived by the build, never stored.",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "isDefinedBy", "objectType"],
  "properties": {
    "id":          { "type": "string", "format": "uri", "description": "Resolvable identity. Immutable; this is the value carried as dictionaryReference." },
    "replaces":    { "type": "string", "format": "uri", "description": "Backward link to the entry this one supersedes. Set at publication, never changed. Optional." },
    "isDefinedBy": { "type": "string", "format": "uri", "description": "Origin dictionary, carried as data (not parsed from the key)." },

    "objectType": {
      "type": "string",
      "enum": [
        "SingleValuedDataElement", "MultiLanguageDataElement", "RelatedResource",
        "MultiValuedDataElement", "DataElementCollection",
        "MeasurementUnit", "Quantity", "Value"
      ]
    },
    "shortName":     { "type": "string" },
    "preferredName": { "$ref": "#/$defs/langMap" },
    "definition":    { "$ref": "#/$defs/langMap" },
    "inheritsFrom":  { "type": "array", "items": { "type": "string", "format": "uri" }, "description": "Entries this entry is derived from (N1355 UML: inheritsFrom)." },
    "identicalTo":   { "type": "array", "items": { "type": "string", "format": "uri" }, "description": "Asserted-equivalent entries in other dictionaries (N1355 UML: identicalTo)." },

    "valueDataType":     { "type": "string" },
    "unit":              { "type": "string", "format": "uri" },
    "exampleValue":      true,
    "enumeration":       { "type": "array", "items": { "type": "string", "format": "uri" } },
    "definitionStandard":{ "$ref": "#/$defs/standardRef", "description": "This entry's reference to its global definition — the authoritative technical standard that defines the concept. `uri` must be version-stable (a dated edition), not a living-document link; the undated designation belongs in `name` only." },
    "testStandard":      { "$ref": "#/$defs/standardRef" },
    "legalBasis":        { "$ref": "#/$defs/standardRef", "description": "The law or regulation that defines or mandates this concept (e.g. an ELI reference), distinct from a technical/testing standard. `uri` should pin to a specific expression (e.g. an Official Journal text), not a live consolidated alias — an immutable entry should not cite a legal text that can change under it." },
    "resourceMediaType": { "type": "string" },
    "itemType":          { "type": "string", "format": "uri" },
    "elements":          { "type": "array", "items": { "$ref": "#/$defs/collectionMember" } },

    "symbol":         { "$ref": "#/$defs/langMap" },
    "quantityKind":   { "type": "string", "format": "uri" },
    "dimension":      { "type": "string" },
    "coherentSiUnit": { "type": "string", "format": "uri" },
    "crossReferences":{ "$ref": "#/$defs/crossReferences" },
    "conversions":    { "type": "array", "items": { "$ref": "#/$defs/conversion" } },

    "value": { "type": ["string", "number", "boolean"] }
  },

  "allOf": [
    { "if": { "required": ["objectType"], "properties": { "objectType": { "const": "SingleValuedDataElement" } } },
      "then": { "required": ["valueDataType"] } },
    { "if": { "required": ["objectType"], "properties": { "objectType": { "const": "MultiLanguageDataElement" } } },
      "then": { "required": ["valueDataType"] } },
    { "if": { "required": ["objectType"], "properties": { "objectType": { "const": "RelatedResource" } } },
      "then": { "required": ["valueDataType"] } },
    { "if": { "required": ["objectType"], "properties": { "objectType": { "const": "MultiValuedDataElement" } } },
      "then": { "required": ["itemType"] } },
    { "if": { "required": ["objectType"], "properties": { "objectType": { "const": "DataElementCollection" } } },
      "then": { "required": ["elements"] } },
    { "if": { "required": ["objectType"], "properties": { "objectType": { "const": "MeasurementUnit" } } },
      "then": { "required": ["symbol"] } },
    { "if": { "required": ["objectType"], "properties": { "objectType": { "const": "MeasurementUnit" } } },
      "then": { "required": ["crossReferences"], "properties": { "crossReferences": { "required": ["ucumCode"] } } } },
    { "if": { "required": ["objectType"], "properties": { "objectType": { "const": "Quantity" } } },
      "then": { "required": ["dimension"] } },
    { "if": { "required": ["objectType"], "properties": { "objectType": { "const": "Value" } } },
      "then": { "required": ["value"] } }
  ],

  "$defs": {
    "langMap": {
      "type": "object",
      "minProperties": 1,
      "propertyNames": { "pattern": "^[a-z]{2,3}(-[A-Z]{2})?$" },
      "additionalProperties": { "type": "string" }
    },
    "standardRef": {
      "type": "object",
      "required": ["name", "uri"],
      "additionalProperties": false,
      "properties": {
        "name":   { "type": "string" },
        "clause": { "type": "string" },
        "uri":    { "type": "string", "format": "uri" }
      }
    },
    "conversion": {
      "type": "object",
      "required": ["toUnit", "factor"],
      "additionalProperties": false,
      "properties": {
        "toUnit": { "type": "string", "format": "uri" },
        "factor": { "type": "number" },
        "offset": { "type": "number" }
      }
    },
    "collectionMember": {
      "type": "object",
      "required": ["dictionaryReference", "isMandatory"],
      "additionalProperties": false,
      "properties": {
        "dictionaryReference": { "type": "string", "format": "uri" },
        "isMandatory":         { "type": "boolean" },
        "accessCategory":      { "type": "string", "format": "uri", "description": "Informative marker of the access category a content specification assigns to this member — a reference to a Value entry of the access-category vocabulary. Membership-level like isMandatory, never a property of the concept entry: the applicable legal act is normative, and enforcement is the passport interface's, not the dictionary's. Purely semantic collections leave it absent." }
      }
    },
    "crossReferences": {
      "type": "object",
      "additionalProperties": true,
      "properties": {
        "uneceCommonCode": { "type": "string" },
        "ucumCode":        { "type": "string", "format": "ucum-code" },
        "qudtUnit":        { "type": "string", "format": "uri" }
      }
    }
  }
}
